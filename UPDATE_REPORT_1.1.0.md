# 圣经体翻译器 1.1.0 更新报告

发布日期：2026-07-25

## 更新目标

本次版本集中解决 300 字以上故事容易反复重试、超时或进入固定兜底的问题，同时保持人物关系、借贷方向、动作对象、否定关系和结局等核心事实的校验强度。

## 主要改进

1. 改进长故事类型识别。没有引号但具有多个人物事件、时间推进和明确结局的长文，现在会进入故事规划流程，不再被当成只有一至三个骨架的普通事实说明。
2. 修复伪人名误判。“把昨日”“有些人虽然”“就把这些”“主管最后查”等句子片段不再被当成人名，也不会因此误杀已经生成的正文。
3. 长故事采用按篇幅变化的结构单元数量，并要求模型省略 JSON 中的空字段，减少结构输出过大、截断和解析失败。
4. DeepSeek 与 Qwen 兼容接口优先关闭不必要的推理过程；若接口不支持对应参数，会自动移除参数并重试，不会直接进入兜底。
5. 根据输入长度分配单次请求时间。长故事获得更完整的一次生成时间，避免两次请求都在固定的短时限处中断。
6. 长故事的事实审查分为硬错误和提示项。一次性配角、次要寒暄或非核心细节的遗漏只会标记为最佳结果，不再毁掉整篇正文。
7. 借贷或交易方向、伤害执行者与对象、否定关系、结局反转和凭空增加冲突仍属于硬错误，必须修正后才能作为正式结果返回。
8. OpenAI 兼容模型协商层继续支持模型名、JSON mode、system role、token 字段、temperature 和推理开关差异；配置错误会直接反馈给用户。

## 真实接口压力测试

使用 300、700、1400 字三档输入，各运行两轮，共六轮。

| 指标 | 优化前 | 1.1.0 |
| --- | ---: | ---: |
| 300 字以上故事兜底次数 | 6 / 6 | 0 / 6 |
| 正常结构化结果 | 0 | 3 |
| 自动修复结果 | 0 | 2 |
| 事实安全的最佳结果 | 0 | 1 |
| 平均响应时间 | 约 38 秒 | 8.4 秒 |
| 最大响应时间 | 约 44 秒 | 13.7 秒 |

详细测试记录位于本地构建目录：`.artifacts/fallback-benchmark-long-v5.json`。

## 自动化验证

- TypeScript 类型检查通过。
- 110 项自动化测试全部通过。
- 覆盖长故事类型判断、伪人名过滤、严重事实分级、模型兼容协商、篇幅控制、格言与故事骨架、分节、桌面版和部署配置。

## 兼容性与安全

- 网站与 Windows 桌面版继续采用 BYOK，发布包不包含任何 API Key。
- 旧的 `DEEPSEEK_*` 环境变量仍然兼容。
- 新增的推理开关会在接口不支持时自动关闭该能力，不影响其他 OpenAI 兼容服务。
- Windows 便携版仍未购买代码签名证书，首次运行可能出现 SmartScreen 的“未知发布者”提示。

## 已知限制

当输入达到 1400 字以上且包含大量重复情节时，系统会优先压缩重复内容并保证返回可用正文，因此可能显示篇幅未完全达到目标的提示；该情况不再进入固定兜底。
