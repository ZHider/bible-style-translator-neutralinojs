# 圣经体翻译器 1.1.1 更新报告

发布日期：2026-07-26

## 更新目标

本次版本重点处理结构生成失败后的恢复与反馈，避免模型异常、超时或结构审查未通过时，用近似原文或固定套话冒充正式翻译结果。

## 主要改进

1. 结构化 JSON 被截断时，会恢复其中已经完整生成且可以解析的结构单元，降低因为末尾截断而整次作废的概率。
2. 两轮结构生成仍未得到可用方案时，系统会进行受事实审查约束的直接生成抢救。
3. 直接生成只有在人物、动作对象、因果和关键事实安全时才可展示；篇幅或次要指标存在偏差时，会明确标记为最佳结果。
4. 所有生成路径均失败时，接口返回明确的超时或生成失败信息，不再返回近似原文的固定保守稿。
5. API Key、余额、限流、模型名称和接口参数错误继续直接反馈给用户，不会被兜底正文掩盖。
6. 发起新请求时立即清除旧正文；请求失败后也保持结果区为空，防止用户把上一次结果误认为本次生成内容。
7. 删除运行时已经停用的紧急兜底正文生成器及 `fallback` 响应类型，降低以后误接回固定兜底的风险。

## 验证范围

- 结构输出截断恢复。
- 结构失败后的直接生成抢救。
- 无法恢复时的 504 错误响应。
- 模型兼容参数协商与明确错误反馈。
- 前端失败状态与旧结果清理。
- TypeScript 类型检查、自动化测试和生产构建。

## 发布说明

- 网站与 Windows 桌面版仍采用 BYOK，不包含任何预置 API Key。
- Windows 便携版未购买代码签名证书，首次运行可能显示“未知发布者”。
